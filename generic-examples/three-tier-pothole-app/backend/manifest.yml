---
applications:
  - name: three-tier-backend
    memory: 128M

    buildpacks:
      - ruby_buildpack

    services:
      - backend-postgres

    routes:
      - route: three-tier-backend.apps.internal

    env:
      RACK_ENV: production
